<%= content_tag( 'p', @session.dates) %>

<%= content_tag( 'p', "Regnal year citation: #{@session.regnal_year_citation}" ) %>

<%= content_tag( 'p', "Wikidata ID: #{link_to( @session.wikidata_id, "https://www.wikidata.org/wiki/#{@session.wikidata_id}" )}".html_safe ) if @session.wikidata_id %>

<%= content_tag( 'h2', 'Months' ) %>
<ol class="unnumbered">
	<%= render :partial => 'month', :collection => @session.month_range %>
</ol>