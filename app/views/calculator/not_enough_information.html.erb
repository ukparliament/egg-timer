<%= render :partial => 'nav' %>

<%= content_tag( 'p', "In order to complete the calculation, we need:" ) %>
<ul>
	<%= content_tag( 'li', "A start date" ) if params['start-date'].nil? %>
	<%= content_tag( 'li', "A day count" ) if params['day-count'].nil? %>
	<%= content_tag( 'li', "A procedure, or a calculation style" )  if params['procedure'].nil? and params['calculation-style'].nil?%>
</ul>